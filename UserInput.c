#include<stdio.h>

void main(){
    double gpa;
    printf("Enter your gpa: ");
    scanf("%lf", &gpa); // scanned user input and stores it in the age variable, %lf means looking for a double
    printf("Your gpa is %f ", gpa);
    
}